this is a table folder

only excel, tsv, csv, or table-like structure here

in the section md, a table must be mentioned in prose before its `<tables/...>` placement line, not just captioned after

table format rules:
1. all cells are vertically centered
2. the header row is bold

there is "temp" folder for temporary files like md, or images to ocr, and all that
